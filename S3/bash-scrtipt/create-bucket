# Check for bucket name
if [ -z "$1" ]; then
    echo "A bucket name is required. Example: ./bucket.sh my-bucket-name"
    exit 1
fi

BUCKET_NAME=$1

aws s3api create-bucket \
    --bucket "$BUCKET_NAME" \
    --region us-east-1
